# Template Portfolio

This repository is a generic portfolio template with placeholder content.

## Stack

* React + Vite
* React Router
* Framer Motion
* CSS Modules
* Strict TypeScript

## Run

1. Install dependencies: `npm install`
2. Start the dev server: `npm run dev`
3. Build for production: `npm run build`
